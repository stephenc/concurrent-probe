= Concurrent Probe

== Introduction

A simple tool that writes some content to a file while simulating doing work
At the end of the simulated work it verifies that the file's content matches 
the content that it generated and exits if there was an issue
